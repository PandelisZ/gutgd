version: '3'

tasks:
  install:frontend:deps:
    dir: ./frontend
    cmds:
      - pnpm install

  dev:frontend:
    dir: ./frontend
    cmds:
      - pnpm dev

  build:frontend:
    dir: ./frontend
    cmds:
      - pnpm build

  generate:bindings:
    cmds:
      - wails3 generate bindings
