version: '3'

tasks:
  build:
    cmds:
      - cmd /c "call .\build\windows\cgo-env.cmd go build -o build\bin\gutgd.exe ."

  run:
    cmds:
      - cmd /c "call .\build\windows\cgo-env.cmd .\build\bin\gutgd.exe"
